import Heading from "../../components/Heading";


const page = () => {
    return (
        <div>
            <Heading title={'Add Jods'} hrefUrll={'../jobs'}/>
        </div>
    );
};

export default page;